# web/ — the reading map UI (Phase 9)

Placeholder so the repository layout matches plan §3.1 from day one and
`make web` has a directory to point at. The Next.js 16 + Tailwind 4 + shadcn v4
+ React Flow app is scaffolded in Phase 9; until then `make web` explains that
instead of failing cryptically.
