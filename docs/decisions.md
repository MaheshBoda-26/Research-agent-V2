# Decisions log

One entry per decision that looked reversible but wasn't (or vice versa).
Newest first.

## 2026-09-22 — LLM provider/model verification

_Placeholder — filled in by Phase 1 Task 1.5 (`scripts/verify_llm.py`) with the
live NIM catalogue evidence and the verified `LLM_MODEL` slug._
